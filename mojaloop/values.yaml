apiVersion: extensions/v1beta1
kind: Ingress

central:
  centralledger:
   spec:
    rules:
     - host: central-ledger-admin-transfer.zared.demo.openafrica.network

  centraldirectory:
   spec:
    rules:
    - host: central-directory.zared.demo.openafrica.network

  centralkms:
   spec:
    rules:
    - host: central-kms.zared.demo.openafrica.network

  centralledger-handler-admin-transfer:
    spec:
     rules:
      - host: central-ledger-admin-transfer.zared.demo.openafrica.network
